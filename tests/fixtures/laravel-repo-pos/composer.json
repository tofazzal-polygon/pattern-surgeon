{
    "name": "fixture/laravel-repo-pos",
    "description": "Pattern-surgeon fixture: Laravel app with Repository smell (raw DB access inline in a service).",
    "type": "project",
    "require": {
        "php": "^8.2",
        "laravel/framework": "^11.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10"
    },
    "autoload": {
        "files": ["src.php"]
    }
}
